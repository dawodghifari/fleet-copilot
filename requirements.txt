# Layer 1 — ML pipeline
pandas>=2.0
scikit-learn>=1.4
xgboost>=2.0
mlflow>=2.10
matplotlib>=3.8
joblib>=1.3
tabulate>=0.9

# Layer 2 — RAG
chromadb>=0.5
sentence-transformers>=3.0

# Layer 3 — agent + API
anthropic>=0.34
fastapi>=0.110
uvicorn>=0.29
python-dotenv>=1.0
